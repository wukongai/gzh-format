interface:
  display_name: "公众号排版"
  short_description: "把 Markdown 长文排成微信公众号兼容 HTML"
  default_prompt: "Use $gzh-format to format this Markdown article as WeChat Official Account HTML, validate it, and generate a local copy preview. Stop before draft creation or publishing."
